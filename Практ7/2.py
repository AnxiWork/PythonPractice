# Функция возвращает максимальный элемент в списке
def max_element(numbers):
    return max(numbers)  # Встроенная функция max делает всю работу

# Проверяем результат на примере
print(max_element([1, 2, 3, 4, 5]))