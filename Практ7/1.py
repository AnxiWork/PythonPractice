# Эта функция считает сумму всех элементов списка
def sum_list(numbers):
    return sum(numbers)  # Используем встроенную функцию sum для подсчета

# Проверяем функцию на примере
print(sum_list([1, 2, 3, 4, 5]))